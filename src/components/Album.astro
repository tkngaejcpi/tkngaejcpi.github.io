---
import Album_ from '@components/Album_';

interface Slide {
  src: string;
  description?: string;
}

interface Props {
  slides: Slide[];
}

const { slides } = Astro.props;
---

<Album_ slides={slides} client:load />

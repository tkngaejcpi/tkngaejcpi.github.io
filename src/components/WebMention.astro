---
import WebMention_ from '@components/WebMention_';

interface Props {
	url: string;
	mastdonRespot?: string;
}

const { url, mastdonRespot } = Astro.props;
---

<WebMention_ url={url} mastodonRepost={mastdonRespot} client:only="react" />
